/**
 * This package contains integration tests, which are executed only inside a docker container.
 *
 * To avoid running them during compilation they are named {@code Integration_}something and not starting or ending with 'Test'.
 */
package javax0.jamal.test.integration;